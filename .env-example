TELEGRAM_BOT_TOKEN=
REDIS_HOST=
DATABASE_FILE=
APP_ENV=dev # dev para sqlite, prod para postgres
OWNER_ID=
SECRET_KEY=
WEBAPP_URL=
WEBHOOK_URL=
# Segredo exclusivo do webhook (1-256 caracteres: A-Z, a-z, 0-9, _ ou -). Troque por um valor unico em cada ambiente.
TELEGRAM_WEBHOOK_SECRET=example_webhook_secret_change_me_2026
CORS_ALLOW_ORIGINS=
JWT_ISSUER=t.me/legendasbrbot
GIN_MODE=release

# MTProto (optional - required for connected accounts feature)
MTPROTO_APP_ID=
MTPROTO_APP_HASH=
# Obtenha em https://my.telegram.org/apps
# A criptografia de sessoes usa a SECRET_KEY do projeto (derivada via SHA-256)
